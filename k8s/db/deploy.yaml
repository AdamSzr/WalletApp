apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: deploydb
  name: deploydb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deploydb
  strategy: {}
  template:
    metadata:
      labels:
        app: deploydb
    spec:
      containers:
      - name: deploydb
        image: default-route-openshift-image-registry.apps.ocp.lab.cloudpak.site/labproj23/postgres-wallet:1.0
        resources: {}
        ports:
        - containerPort: 5432
        envFrom:
        - configMapRef:
            name: dbcm
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: dbsecret
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dbsecret
              key: POSTGRES_PASSWORD
        volumeMounts:
        - mountPath: /data
          name: dbvolume
      volumes:
      - name: dbvolume
        persistentVolumeClaim:
          claimName: postgrespvc
status: {}


# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   labels:
#     app: db-deploy
#   name: db-deploy
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: db-deploy
#   strategy: {}
#   template:
#     metadata:
#       labels:
#         app: db-deploy
#     spec:
#       containers:
#       - name: db-deploy
#         image: image-registry.openshift-image-registry.svc:5000/labproj22/postgres:14.1
#         resources: {}
#         ports:
#         - containerPort: 5432
#         envFrom:
#         - configMapRef:
#             name: db-cmap
#         env:
#         - name: USER_DB
#           valueFrom:
#             secretKeyRef:
#               name: db-secret
#               key: USER_DB
#         - name: POSTGRES_PASSWORD
#           valueFrom:
#             secretKeyRef:
#               name: db-secret
#               key: PASSWORD_DB
#         volumeMounts:
#         - mountPath: /data
#           name: postgredb
#       volumes:
#       - name: postgredb
#         persistentVolumeClaim:
#           claimName: db-pvc
# status: {}